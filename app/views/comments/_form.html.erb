<div>コメントを投稿する</div>
	<%= form_with(model: [comment], url: comments_path(params[:id])) do |f| %>
	    <%= f.hidden_field :post_id,value:post.id %>
		<%= f.text_field :body, class: "col s9 form-control form-control-lg w-100 comment-body" %>
	<%= f.submit "投稿", class:" btn btn-small" %>
<% end %>


